
class IServiceTemplate {
    iServiceName = (domainServicesLocal, snackCase) => `${domainServicesLocal}/i_${snackCase}.service.dart`
    iServiceTemplate = (pascalCase) => `
import 'package:meta/meta.dart';
abstract class I${pascalCase}Service {
}
`
}

module.exports = IServiceTemplate;
