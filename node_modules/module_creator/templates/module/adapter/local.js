const adapterLocal = (interfaceLocal) => `${interfaceLocal}/index.dart`
const adapterTemplate = `
export  'protocols/index.dart';
export  'services/index.dart';
`

const modelsIndexLocal = (modelsLocal) => `${modelsLocal}/index.dart`
const serviceIndexLocal = (servicesLocal) => `${servicesLocal}/index.dart`
const externalIndexLocal = (externalLocal) => `${externalLocal}/index.dart'`

class AdapterIndex {

    adapterLocal = (interfaceLocal) => `${interfaceLocal}/index.dart`
    adapterTemplate = `
export  'protocols/index.dart';
export  'services/index.dart';
`

}

module.exports = AdapterIndex;