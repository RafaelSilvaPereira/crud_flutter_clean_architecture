class Util {
    toSnakeCase(text) {

        return text.trim().toLowerCase().split(' ').join('_');
    }

    toPascalCase(text) {
        return text.trim().replace(/\w\S*/g, m => m.charAt(0).toUpperCase() + m.substr(1).toLowerCase()).split(' ').join('');
    }
}

module.exports = Util